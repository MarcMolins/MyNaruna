import React from 'react';
import ReactDOM from 'react-dom/client';
import MyNaruna from './MyNaruna';


const content = ReactDOM.createRoot(document.getElementById('content'));
content.render(
  <React.StrictMode>
    <MyNaruna />
  </React.StrictMode>
);
